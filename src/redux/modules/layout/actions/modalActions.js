import { createAction } from 'redux-actions'

export const SET_MODAL_WINDOW_OPEN = 'SET_MODAL_WINDOW_OPEN';

/* CHANGE LANGUAGE NOTIFIER */

export const setModalWindowOpen = createAction(SET_MODAL_WINDOW_OPEN);
